# Samples folder

This directory is intentionally empty in the handover package.

Place here the per-tenant Excel workbooks produced by `lp_tenant_exporter_v2`, for example:

- `tenant_a_config.xlsx`
- `tenant_b_config.xlsx`

These workbooks contain customer configuration and must be treated as confidential.
